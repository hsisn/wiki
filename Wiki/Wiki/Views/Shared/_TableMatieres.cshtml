@model IEnumerable<Wiki.Models.Biz.Article>

<table class="table">

@foreach (var item in Model) {
    <tr>
        <td>

            @Html.ActionLink(@Html.DisplayFor(modelItem => item.Titre).ToString(), "Details", new { /* id=item.PrimaryKey */ })

        </td>
    </tr>
}

</table>
